<Window x:Class="Equalizer.Views.EqualizerSettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:theme="clr-namespace:YukkuriMovieMaker.Theme;assembly=YukkuriMovieMaker.Plugin"
        xmlns:cv="clr-namespace:Equalizer.Converters"
        xmlns:vm="clr-namespace:Equalizer.ViewModels"
        xmlns:localization="clr-namespace:Equalizer.Localization"
        d:DataContext="{d:DesignInstance Type=vm:EqualizerSettingsViewModel}"
        mc:Ignorable="d"
        Title="{x:Static localization:Texts.EqualizerSettingsTitle}" Height="500" Width="800"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource {x:Static SystemColors.ControlBrushKey}}"
        Foreground="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}">
    <Window.Resources>
        <cv:NotNullToBooleanConverter x:Key="NotNullConverter"/>
        <cv:BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
        <cv:FavoriteToColorConverter x:Key="FavoriteToColorConverter"/>

        <Style TargetType="TabControl" x:Key="ModernTabControlStyle">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TabControl">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>
                            <TabPanel x:Name="HeaderPanel" Grid.Row="0" IsItemsHost="True" Background="Transparent"/>
                            <Border x:Name="ContentPanel" Grid.Row="1"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    Background="{TemplateBinding Background}">
                                <ContentPresenter x:Name="PART_SelectedContentHost"
                                                  ContentSource="SelectedContent"
                                                  SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>
                            </Border>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="TabItem">
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="Foreground" Value="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TabItem">
                        <Border x:Name="Border"
                                BorderThickness="0,0,0,2"
                                BorderBrush="Transparent"
                                Background="Transparent"
                                Margin="0,0,10,0">
                            <ContentPresenter x:Name="ContentSite"
                                              VerticalAlignment="Center"
                                              HorizontalAlignment="Center"
                                              ContentSource="Header"
                                              Margin="{TemplateBinding Padding}">
                                <ContentPresenter.Resources>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Foreground" Value="{Binding RelativeSource={RelativeSource AncestorType=TabItem}, Path=Foreground}"/>
                                    </Style>
                                </ContentPresenter.Resources>
                            </ContentPresenter>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Border" Property="BorderBrush" Value="{DynamicResource {x:Static SystemColors.HighlightBrushKey}}"/>
                                <Setter Property="Foreground" Value="{DynamicResource {x:Static SystemColors.HighlightBrushKey}}"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Foreground" Value="{DynamicResource {x:Static SystemColors.HighlightBrushKey}}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ActionButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource {x:Static SystemColors.ControlDarkBrushKey}}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="3"
                                SnapsToDevicePixels="True">
                            <ContentPresenter x:Name="contentPresenter"
                                              Focusable="False"
                                              HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                              Margin="{TemplateBinding Padding}"
                                              RecognizesAccessKey="True"
                                              SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"
                                              VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="{DynamicResource {x:Static SystemColors.HighlightBrushKey}}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{DynamicResource {x:Static SystemColors.ControlDarkBrushKey}}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Opacity" Value="0.56"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="IconButtonStyle" TargetType="Button" BasedOn="{StaticResource ActionButtonStyle}">
            <Setter Property="Padding" Value="4"/>
        </Style>

        <Style x:Key="ScrollBarThumbStyle" TargetType="{x:Type Thumb}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Thumb}">
                        <Border x:Name="rectangle"
                                SnapsToDevicePixels="True"
                                Background="{DynamicResource {x:Static SystemColors.ControlDarkBrushKey}}"
                                CornerRadius="3"
                                Opacity="0.6"/>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="rectangle" Property="Opacity" Value="0.8"/>
                            </Trigger>
                            <Trigger Property="IsDragging" Value="True">
                                <Setter TargetName="rectangle" Property="Opacity" Value="1"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="{x:Type ScrollBar}">
            <Setter Property="Stylus.IsPressAndHoldEnabled" Value="False"/>
            <Setter Property="Stylus.IsFlicksEnabled" Value="False"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Width" Value="8"/>
            <Setter Property="MinWidth" Value="8"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ScrollBar}">
                        <Grid SnapsToDevicePixels="True" Background="{TemplateBinding Background}">
                            <Track x:Name="PART_Track" IsDirectionReversed="True" IsEnabled="{TemplateBinding IsMouseOver}">
                                <Track.Thumb>
                                    <Thumb Style="{DynamicResource ScrollBarThumbStyle}"/>
                                </Track.Thumb>
                            </Track>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="Orientation" Value="Horizontal">
                    <Setter Property="Width" Value="Auto"/>
                    <Setter Property="MinWidth" Value="0"/>
                    <Setter Property="Height" Value="8"/>
                    <Setter Property="MinHeight" Value="8"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="{x:Type ScrollBar}">
                                <Grid SnapsToDevicePixels="True" Background="{TemplateBinding Background}">
                                    <Track x:Name="PART_Track" IsEnabled="{TemplateBinding IsMouseOver}">
                                        <Track.Thumb>
                                            <Thumb Style="{DynamicResource ScrollBarThumbStyle}"/>
                                        </Track.Thumb>
                                    </Track>
                                </Grid>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="GroupListBoxItemStyle" TargetType="ListBoxItem">
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBoxItem">
                        <Border Name="Border" Padding="{TemplateBinding Padding}" Background="Transparent" SnapsToDevicePixels="True">
                            <TextBlock Text="{Binding Name}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#19000000"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="{DynamicResource {x:Static SystemColors.HighlightBrushKey}}"/>
                                <Setter Property="Foreground" Value="{DynamicResource {x:Static SystemColors.HighlightTextBrushKey}}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="FavoriteButtonStyle" TargetType="Button">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="Transparent" BorderBrush="Transparent" BorderThickness="0">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="10">
        <TabControl Style="{StaticResource ModernTabControlStyle}">
            <TabItem Header="{x:Static localization:Texts.BasicSettingsTab}">
                <StackPanel Margin="10">
                    <TextBlock Text="{x:Static localization:Texts.ProcessingAlgorithm}" FontWeight="Bold" Margin="0,0,0,5"/>
                    <ComboBox ItemsSource="{Binding AlgorithmOptions}"
                              SelectedItem="{Binding SelectedAlgorithm, Mode=TwoWay}"
                              Margin="0,0,0,20" Height="30" VerticalContentAlignment="Center"/>

                    <CheckBox IsChecked="{Binding HighQualityMode}"
                              Content="{x:Static localization:Texts.EnableHighQualityMode}"
                              Margin="0,0,0,10"
                              Foreground="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}">
                        <CheckBox.ToolTip>
                            <ToolTip>
                                <TextBlock>
                                    <Run Text="{x:Static localization:Texts.HighQualityTooltipLine1}"/><LineBreak/>
                                    <Run Text="{x:Static localization:Texts.HighQualityTooltipLine2}"/>
                                </TextBlock>
                            </ToolTip>
                        </CheckBox.ToolTip>
                    </CheckBox>
                    <TextBlock Text="{x:Static localization:Texts.HighQualityCpuNote}" FontSize="10" Margin="20,0,0,20" Opacity="0.7"/>

                    <TextBlock Text="{x:Static localization:Texts.DefaultPreset}" FontWeight="Bold" Margin="0,0,0,5"/>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <ComboBox ItemsSource="{Binding AllPresetNames}"
                                  SelectedItem="{Binding SelectedDefaultPreset, Mode=TwoWay}"
                                  Margin="0,0,10,0" Height="30" VerticalContentAlignment="Center"/>
                        <Button Grid.Column="1" Content="{x:Static localization:Texts.Clear}" Width="60" Height="30"
                                Command="{Binding ClearDefaultPresetCommand}"
                                ToolTip="{x:Static localization:Texts.ClearDefaultPresetTooltip}"
                                Style="{StaticResource ActionButtonStyle}"/>
                    </Grid>
                    <TextBlock Text="{x:Static localization:Texts.DefaultPresetDescription}"
                               FontSize="10" Opacity="0.7" Margin="0,5,0,0"/>
                </StackPanel>
            </TabItem>

            <TabItem Header="{x:Static localization:Texts.PresetManagementTab}">
                <Grid Margin="10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="200"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <Grid Grid.Column="0">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <TextBlock Grid.Row="0" Text="{x:Static localization:Texts.PresetGroup}" FontWeight="Bold" Margin="0,0,0,5"/>
                        <ListBox Grid.Row="1" ItemsSource="{Binding Groups}"
                                 SelectedItem="{Binding SelectedGroupItem}"
                                 ItemContainerStyle="{StaticResource GroupListBoxItemStyle}"/>

                        <Grid Grid.Row="2" Margin="0,5,0,0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Button Grid.Column="0" Command="{Binding AddGroupCommand}" Height="28" Margin="0,0,2,0"
                                    Style="{StaticResource IconButtonStyle}" ToolTip="{x:Static localization:Texts.AddGroup}">
                                <Viewbox>
                                    <Path Data="M19,13H13V19H11V13H5V11H11V5H13V11H19V13Z"
                                          Fill="{Binding RelativeSource={RelativeSource AncestorType=Button}, Path=Foreground}" Stretch="Uniform"/>
                                </Viewbox>
                            </Button>
                            <Button Grid.Column="1" Command="{Binding DeleteGroupCommand}" Height="28" Margin="2,0,2,0"
                                    Style="{StaticResource IconButtonStyle}" ToolTip="{x:Static localization:Texts.DeleteSelectedGroup}">
                                <Viewbox>
                                    <Path Data="M19,13H5V11H19V13Z"
                                          Fill="{Binding RelativeSource={RelativeSource AncestorType=Button}, Path=Foreground}" Stretch="Uniform"/>
                                </Viewbox>
                            </Button>
                            <Button Grid.Column="2" Command="{Binding MoveGroupUpCommand}" Height="28" Margin="2,0,2,0"
                                    Style="{StaticResource IconButtonStyle}" ToolTip="{x:Static localization:Texts.MoveUp}">
                                <Viewbox>
                                    <Path Data="M7.41,15.41L12,10.83L16.59,15.41L18,14L12,8L6,14L7.41,15.41Z"
                                          Fill="{Binding RelativeSource={RelativeSource AncestorType=Button}, Path=Foreground}" Stretch="Uniform"/>
                                </Viewbox>
                            </Button>
                            <Button Grid.Column="3" Command="{Binding MoveGroupDownCommand}" Height="28" Margin="2,0,0,0"
                                    Style="{StaticResource IconButtonStyle}" ToolTip="{x:Static localization:Texts.MoveDown}">
                                <Viewbox>
                                    <Path Data="M7.41,8.59L12,13.17L16.59,8.59L18,10L12,16L6,10L7.41,8.59Z"
                                          Fill="{Binding RelativeSource={RelativeSource AncestorType=Button}, Path=Foreground}" Stretch="Uniform"/>
                                </Viewbox>
                            </Button>
                        </Grid>
                    </Grid>

                    <Grid Grid.Column="1" Margin="10,0,0,0">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <Grid Grid.Row="0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="{x:Static localization:Texts.PresetList}" FontWeight="Bold" VerticalAlignment="Center"/>
                            <Button Grid.Column="1" Content="{x:Static localization:Texts.Import}"
                                    Command="{Binding ImportCommand}" Margin="0,0,10,0"
                                    Style="{StaticResource ActionButtonStyle}"
                                    ToolTip="{x:Static localization:Texts.ImportPresetTooltip}"/>
                            <Button Grid.Column="2" Content="{x:Static localization:Texts.Export}"
                                    Command="{Binding ExportCommand}"
                                    Style="{StaticResource ActionButtonStyle}"
                                    ToolTip="{x:Static localization:Texts.ExportSelectedPresetTooltip}"/>
                        </Grid>

                        <ListBox Grid.Row="1" Margin="0,10,0,0"
                                 ItemsSource="{Binding Presets}"
                                 SelectedItem="{Binding SelectedPreset}">
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <Grid Height="36">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <Button Grid.Column="0"
                                                Width="20" Height="20" Margin="0,0,5,0"
                                                Style="{StaticResource FavoriteButtonStyle}"
                                                Command="{Binding DataContext.ToggleFavoriteCommand, RelativeSource={RelativeSource AncestorType=ListBox}}"
                                                CommandParameter="{Binding}"
                                                ToolTip="{x:Static localization:Texts.ToggleFavorite}">
                                            <Path Data="M12,17.27L18.18,21L17.27,14.41L22,9.82L15.77,8.89L12,3L8.23,8.89L2,9.82L6.73,14.41L5.82,21L12,17.27Z"
                                                  Fill="{Binding IsFavorite, Converter={StaticResource FavoriteToColorConverter}}"
                                                  Stretch="Uniform" Width="12" Height="12"/>
                                        </Button>
                                        <TextBlock Grid.Column="1" Text="{Binding Name}"
                                                   VerticalAlignment="Center" Margin="5,0" FontSize="14"/>
                                        <TextBlock Grid.Column="2" Text="{Binding Group}"
                                                   FontSize="10"
                                                   Foreground="{DynamicResource {x:Static SystemColors.GrayTextBrushKey}}"
                                                   VerticalAlignment="Center"/>
                                    </Grid>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>

                        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
                            <Button Content="{x:Static localization:Texts.RenameShort}" Command="{Binding RenameCommand}" Margin="0,0,10,0"
                                    Style="{StaticResource ActionButtonStyle}"/>
                            <Button Content="{x:Static localization:Texts.ChangeGroupShort}" Command="{Binding ChangeGroupCommand}" Margin="0,0,10,0"
                                    Style="{StaticResource ActionButtonStyle}"/>
                            <Button Content="{x:Static localization:Texts.Delete}" Command="{Binding DeleteCommand}"
                                    Style="{StaticResource ActionButtonStyle}"/>
                        </StackPanel>
                    </Grid>
                </Grid>
            </TabItem>
        </TabControl>
    </Grid>
</Window>
